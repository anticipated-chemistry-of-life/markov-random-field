"""Reading the mixing cost of dropping the block update out of two runs.

`traces` reads the joint density trace, `posteriors` reads each tree field's posterior.
`mixing_cost/run.sh` produces the runs both of them read, and `mixing_cost/findings.md` is the
argument they were built to settle.
"""
